# Candidate Selection Validator

The validator requires a complete permutation of the current candidate IDs and selected_policy_id equal to ranking[0]. High confidence accepts any valid supplied option; medium confidence is limited to the deterministic top two; low confidence falls back to the current deterministic recommendation.
